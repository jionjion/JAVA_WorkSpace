package junit.test;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import junit.src.Compute;

public class ComputeTestsda {

	/**使用单元测试,完成断言测试*/
	@Test
	public void	 testAdd() {
		//简单的求和计算, 3 ?= 1 + 2 
		assertEquals(3, new Compute().add(1, 2));
	}
}
