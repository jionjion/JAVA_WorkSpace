package fileReadOrWrite.RandomAccessFileType;

import java.io.File;

/**
 * 	支持对文件的访问,支持编写文件
 * 	支持随机访问文件,支持访问文件的位置
 * 	打开文件的方式:	只读或者读写
 * 	打开文件时,需要指明指针位置和打开模式
 * 	文件模型:一组特定的byte字节数组
 * 	写:一次只写一个字节,同时指针后移
 * 	读:一次只读一个字节
 * 	读写完成后一定要关闭*/
public class RandomAccessFileDemol {

	public static void main(String[] args) throws Exception{
		File new = new File("fileReadOrWrite.RandomAccessFileType.FA");
		
		
	}
}
