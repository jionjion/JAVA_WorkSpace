package com.model.proxy.dynamical.proxy;

import com.model.proxy.dynamical.proxy.InvocationHandler;
import com.model.proxy.dynamical.proxy.Movable;
import java.lang.reflect.Method;

public class $Proxy0 implements Movable {

   private InvocationHandler handler;


   public $Proxy0(InvocationHandler var1) {
      this.handler = var1;
   }

   public void move() {
      try {
         Method var1 = Movable.class.getMethod(" move", new Class[0]);
         this.handler.invoke(this, var1);
      } catch (Exception var2) {
         var2.printStackTrace();
      }

   }
}