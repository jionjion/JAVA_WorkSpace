使用Java开发邮件

javamail.jar

发送协议:
	SMTP协议:简单邮件传送协议
		默认端口号25
	POP3协议:邮局协议3
		默认端口号110

邮件收发过程
	首先是客户端发送邮件到邮箱服务器(SMTP)中,随后邮件发送到对方邮件服务器(SMTP),随后通过(POP3)服务器最终将邮件发送给对方
	
配置安装
	1.配置邮箱服务器
	2.发送邮件
	
邮箱验证:通过在邮件中发送UUID,保存数据库,后期收到用户携带UUID验证,完成用户的注册登记